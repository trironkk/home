return {
		"sphamba/smear-cursor.nvim",
		opts = {
			-- [0, 1] (defaults to 0.6)
			stiffness = 0.8,
			-- [0, 1] (defaults to 0.3)
			trailing_stiffness = 0.5,
			-- [0, inf] (defaults to 0.1)
			distance_stop_animating = 0.5,
			-- true/false (defaults to true)
			hide_target_hack = false,
		},
	}
